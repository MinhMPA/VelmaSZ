import numpy as np
import scipy

class Likelihood():
    """
    Base class for all likelihoods
    """

    def __init__(self, observed_data, model, covariance=None):


        return True

    def estimate_covariance(simulated_data):
        """
        Estimate the covariance from data vector from simulations
        """

        self.covariance = 

        return True

    def estimate_mean(self):
        """
        Estimate the ensemble mean 
        """

        return mean

    def estimate_std(self): 
        """
        Estimate the ensemble std, i.e. the uncertainty on the estimated mean
        """

        return std
